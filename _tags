# --------------------------------------------------------------------
true : use_menhir, menhir_explain
true : debug
true : warn_Z, warn_Y, warn_+28, warn_-23

# --------------------------------------------------------------------
<src>     : include
<src/phl> : include

<why3>   : -traverse
<_tools> : -traverse
<system> : -traverse

# --------------------------------------------------------------------
<src/*.{ml,mli}> : package(menhirLib), package(why3)
<src/*.{native,byte}> : package(menhirLib), package(why3)
