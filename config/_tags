# --------------------------------------------------------------------
true : use_menhir, menhir_explain
true : debug
true : warn_Z, warn_Y, warn_+28

# --------------------------------------------------------------------
<src> or <pprint> : include

# --------------------------------------------------------------------
<src/*.{ml,mli}> : pkg_menhirLib, pkg_why3, pkg_threads
<src/*.{native,byte}> : pkg_menhirLib, pkg_why3, pkg_threads
