;;; this is a prelude for Simplify














(BG_PUSH
 ;; axiom match_bool_T
 (FORALL (a)
 (FORALL (z z1)
 (EQ (sort a (match_bool (sort bool T) (sort a z) (sort a z1))) (sort a z)))))


(BG_PUSH
 ;; axiom match_bool_F
 (FORALL (a)
 (FORALL (z z1)
 (EQ (sort a (match_bool (sort bool F) (sort a z) (sort a z1))) (sort a z1)))))




(BG_PUSH
 ;; axiom index_bool_T
 (EQ (index_bool (sort bool T)) 0))


(BG_PUSH
 ;; axiom index_bool_F
 (EQ (index_bool (sort bool F)) 1))


(BG_PUSH
 ;; axiom bool_inversion
 (FORALL (u)
 (OR (EQ (sort bool u) (sort bool T)) (EQ (sort bool u) (sort bool F)))))


;; G1
 ;; File "list1.why", line 3, characters 7-9
(NOT (EQ (sort bool T) (sort bool F)))
