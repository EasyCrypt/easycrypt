module M

  use import int.Int
  use import real.RealInfix

let f (x:int) = { }
  x + 1
{ result = x + 1 }

let g (x:real) = { }
  -. x +. 1.0
{ result = -. x +. 1.0 }


end

(*
Local Variables:
compile-command: "unset LANG; make -C ../.. examples/programs/reals"
End:
*)
